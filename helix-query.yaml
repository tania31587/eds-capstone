version: 1

indices:
  default:
    include:
      - /**

    exclude:
      - /nav/**
      - /footer/**
      - /fragments/**
      - /drafts/**

    target: /query-index.json

    properties:
      path:
        select: none
        value: path